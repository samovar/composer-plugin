# My composer plugin

Configure symfony application

```shell script
# https://symfony.com/download
symfony new --full app && cd app && bin/phpunit && composer configure --no-backup && vendor/bin/phpstan analyze
```
